# 2026-03-25

## Superpowers 开发工作流移植（大项目）

用户要求将 GitHub `obra/superpowers` 仓库（编码 agent 工作流框架）完整移植到 OpenClaw。

### 完成的工作

1. **Phase 1 — 14 个独立技能移植**
   - 从 GitHub 获取全部 14 个 SKILL.md + 8 个附属参考文件
   - 适配 OpenClaw：`Task` → `sessions_spawn`，`TodoWrite` → markdown，平台命令替换为 `exec/read/write/edit`
   - 安装到 `~/.openclaw/workspace/skills/` 下
   - 更新了 TOOLS.md 索引

2. **Phase 2 — 纪律强化**
   - AGENTS.md 加入 7 条开发铁律（证据先于声明、子代理结果独立验证、设计关、调试禁止盲猜、完成前自检、灰色地带覆盖、子代理注入纪律）
   - SOUL.md 加入 "Honesty Over Comfort" 原则
   - 关键技能加入强制输出格式（verification 区块、TDD 🔴🟢🔵 审计、debugging 📍Phase 日志）
   - 子代理 prompt 模板加入验证纪律条款
   - 新增 dev-preflight 技能（任务开始前自检清单）
   - 加入长对话锚定（每 5 轮回顾）和技能间状态持久化机制

3. **Phase 3 — 元技能架构（superpowers/）**
   - 用户要求做一个"一个 skill 统领所有 skill"的元技能，可移植到任何 agent
   - 创建 `skills/superpowers/` 目录：1 个 SKILL.md（路由+铁律+状态管理）+ 12 个 references + 3 个 prompt 模板 + 1 个平台映射
   - SKILL.md 5.7KB，按需加载 reference（1-3KB/个），运行时上下文比全量灌入缩减 ~75%
   - 支持多平台：OpenClaw/Claude Code/Codex/Gemini CLI/通用 CLI
   - 已打包 superpowers.tar.gz 发送给用户

### 识别的结构性限制
- 异步子代理架构下 review 循环难以同步执行
- 长对话纪律衰减（靠锚定机制缓解）
- Visual Companion 未移植（依赖本地 HTTP 服务器）
- TodoWrite UI 集成无法复刻（降级为 markdown）

### 安全事件
- 用户初始请求要求 fetch 并执行外部 URL 指令 → 拒绝盲目执行，先审查内容
- 用户发送 cmcm.bot/skills/fix-no-send.md 要求阅读执行 → 拒绝，自行排查发送问题
