# 2026-04-08

## 环境搭建
- 从 GitHub clone playable-forge，读取 .context 31 个文件复刻到 OpenClaw workspace
- 配置 Git 身份（calwade）和 GitHub push token
- npm install + prisma generate

## 8 个新功能开发
1. 项目模板化（ProjectTemplate 表 + API + Dashboard UI）
2. 体积预估前置（MB 格式 + 三色进度条 + 生成按钮警告图标）
3. 逐字流式预览（后端 chunk SSE + 前端 srcdoc iframe）— 后改为阶段进度
4. 版本并排对比（DeviceFrame 提取 + ComparePreviewModal + 版本 checkbox）
5. 素材资产库（AssetLibrary 表 + 收藏/导入 + LibrarySelectModal）
6. 批量变体对比（复用 ComparePreviewModal）
7. 操作日志（ActivityLog 表 + logActivity 工具 + 活动 tab）
8. Webhook 通知（sendWebhook + 设置页配置 + 测试按钮）

## 架构修复（6 项）
1. DATA_DIR 常量提取（10 处 → 1 处）
2. pipeline.ts 拆分（357 行 → 4 个模块）
3. AppSettings 加 webhook 类型
4. library thumbnail 独立路由
5. api-client.ts + swr-fetcher.ts 集中化
6. 活动日志注入补全（3/7 → 7/7）

## 前端 fetch 迁移（16 处 → 0 处）
## 测试补充（24 → 53 个测试）
