# 2026-04-09 ~ 2026-04-10

## Bug 修复
- settings 不生效：provider.ts 直接读 env → 改为优先读 settings.json
- provider.chat() 强制 /v1/chat/completions（@ai-sdk/openai 新版默认走 /responses）
- 100% 触发自修复：给 AI 完整 MRAID 模板 + 校验规则降级
- SWR token guards 恢复（防止未认证 401 缓存）
- SimpleTabs 高度链修复（flex flex-col 代替 overflow-hidden）
- 版本/活动列表不显示：用户未跑 npx prisma db push

## 新功能
- 设置页显示默认 System Prompt + 可编辑 + 恢复默认
- 调试面板显示 API URL 和模型名
- API Key 设置页可配置
- 对话 UI 大改（多行 textarea、气泡+头像、GeneratePanel 收起/展开）
- 流式进度（删除半成品 iframe → 阶段清单+字符计数+完成淡入）
- "存为模板"按钮移到项目详情底栏

## 架构改进
- AI 分类独立配置（AIOverrides + getAIConfig(purpose)）
- 视觉分类（发缩略图给 AI，多模态）
- 手动分类触发（删除上传自动分类 + 删除 filename fallback）
- 效果参考图独立（ReferenceImage 表 + 独立上传区）
- 分类不再推断变体角色
- 清理 variantRole 死代码

## 外部审查修复（6 条）
1. 假 Google Play URL → example.com
2. 外部引用白名单删除
3. mraid-open 只认 mraid.open()，升级为 error
4. Asset.variantRole 死字段清理
5. streamPreview flag 删除
6. PLACEHOLDER 检测简化

## 布局优化
- 顶部/底部栏收窄，素材栏加宽
